{
  "name": "netflix",
  "version": "0.1.0",
  "private": true,
  "main": "bootstrap.js",
  "scripts": {
    "package-darwin": "$(npm bin)/electron-packager . Netflix --platform=darwin --arch=x64 --icon=./src/public/netflix.icns --app-version=0.1.0 --app-bundle-id=no.lassebrenden.netflix --helper-bundle-id=no.lassebrenden.netflix.helper --out ./dist --overwrite",
    "start": "$(npm bin)/electron ."
  },
  "engines": {
    "node": ">=8.1.2"
  },
  "devDependencies": {
    "electron": "~>7.2.4",
    "electron-packager": "^8.7.1"
  },
  "dependencies": {
    "babel-core": "^6.25.0",
    "babel-preset-es2015": "^6.24.1",
    "electron-compile": "^6.4.1",
    "electron-compilers": "^5.9.0"
  }
}
